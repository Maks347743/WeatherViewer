<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/search_view"
        android:icon="@drawable/ic_menu_search"
        android:title="@string/search_hint_text"
        app:actionViewClass="android.support.v7.widget.SearchView"
        app:showAsAction="collapseActionView|always"/>

    <item
        android:id="@+id/add_to_favorite_item"
        android:icon="@drawable/ic_menu_heart"
        android:title="@string/add_to_favorite_text"
        app:showAsAction="ifRoom"
        />

    <item
        android:id="@+id/favorite_cities_item"
        android:title="@string/favorite_cities_item_text"
        app:showAsAction="never"
        />

</menu>